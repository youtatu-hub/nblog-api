<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.net.perfect.storage.domain.log.mapper.Dt50BaseLogMapper">

    <select id="streamingQueryContinuousInfo">
        select insert_time as insertTime, log_time as logTime, tid as tid, groupId as groupId from log
        <where>
            <if test="null != tid">
                and tid = #{tid}
            </if>
            <if test="null != startTime">
                <![CDATA[ AND log_time >= #{startTime} ]]>
            </if>
            <if test="null != endTime">
                <![CDATA[ AND log_time <= #{endTime} ]]>
            </if>
            order by log_time asc
        </where>
    </select>

</mapper>